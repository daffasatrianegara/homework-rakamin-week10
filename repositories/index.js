const moviesRepo = require('./movies.repositories')
const usersRepo = require('./users.repositories')
const authRepo = require('./auth.repositories')

module.exports = {
    moviesRepo,
    usersRepo,
    authRepo,
}